/**
 * 解析dom
 */

const cheerio = require('cheerio');


/**
 * 解析 - question - 页面结构
 * @param {*} html 
 */
async function analysisQuestionPage(html) {
  const $ = cheerio.load(html);
}

module.exports = {

};
